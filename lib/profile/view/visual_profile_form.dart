import 'package:flutter/material.dart';

class VisualProfileForm extends StatelessWidget {
  const VisualProfileForm({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
       child: Text('VISUAL PROFILE FORM HERE'), 
    );
  }
}